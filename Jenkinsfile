pipeline {
    agent { label 'any' }
    environment {
    
    stages {
        stage('Deploying') {
            steps {
                    echo "We are deploying" 
					}
            }
        }
    }
